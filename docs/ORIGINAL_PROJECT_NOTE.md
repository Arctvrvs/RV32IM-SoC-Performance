# Original uploaded project

The uploaded `RV32IM.zip` contained a CMake C++17 executable whose `main.cpp` only printed `Hello, World!`.

This starter replaces that placeholder executable with the functional/reference-model entry point and expands the project into the directories in this ZIP. No existing RTL files were overwritten because the uploaded ZIP did not contain RTL sources.
